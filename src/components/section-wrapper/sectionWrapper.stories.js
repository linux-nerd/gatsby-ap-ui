import React from 'react';

import { storiesOf } from '@storybook/react';
import Section from './SectionWrapper';

storiesOf('Components/SectionWrapper', module).add('SectionWrapper component', () => <Section> some content</Section>);